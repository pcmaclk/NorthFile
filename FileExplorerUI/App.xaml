<?xml version="1.0" encoding="utf-8"?>
<Application
    x:Class="FileExplorerUI.App"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:FileExplorerUI.Controls"
    xmlns:local="using:FileExplorerUI">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <XamlControlsResources xmlns="using:Microsoft.UI.Xaml.Controls" />
                <!-- Other merged dictionaries here -->
            </ResourceDictionary.MergedDictionaries>
            <ResourceDictionary.ThemeDictionaries>
                <ResourceDictionary x:Key="Light">
                    <StaticResource x:Key="TitleBarActiveTabPillBackgroundBrush" ResourceKey="LayerFillColorDefaultBrush" />
                    <LinearGradientBrush x:Key="ActivePaneBorderBrush" MappingMode="Absolute" StartPoint="0,0" EndPoint="0,4">
                        <GradientStop Color="#08000000" Offset="0" />
                        <GradientStop Color="#08000000" Offset="1" />
                    </LinearGradientBrush>
                    <StaticResource x:Key="ActivePaneSurfaceBackgroundBrush" ResourceKey="LayerFillColorDefaultBrush" />
                    <StaticResource x:Key="InactivePaneSurfaceBackgroundBrush" ResourceKey="LayerOnMicaBaseAltFillColorSecondaryBrush" />
                    <StaticResource x:Key="AddressSearchChromeBorderBrush" ResourceKey="ControlStrokeColorOnAccentDisabledBrush" />
                    <StaticResource x:Key="InactivePaneBorderBrush" ResourceKey="ControlStrokeColorOnAccentDisabledBrush" />
                    <StaticResource x:Key="ActivePaneInputBackgroundBrush" ResourceKey="TextControlBackground" />
                    <StaticResource x:Key="InactivePaneInputBackgroundBrush" ResourceKey="ControlFillColorSecondaryBrush" />
                    <StaticResource x:Key="ActivePaneForegroundBrush" ResourceKey="TextFillColorPrimaryBrush" />
                    <StaticResource x:Key="InactivePaneForegroundBrush" ResourceKey="TextFillColorSecondaryBrush" />
                    <StaticResource x:Key="DetailsHeaderSecondaryForegroundBrush" ResourceKey="TextFillColorSecondaryBrush" />
                    <SolidColorBrush x:Key="SidebarGroupSeparatorBrush" Color="#22000000" />
                    <LinearGradientBrush x:Key="ExplorerShellPanelBorderBrush" MappingMode="Absolute" StartPoint="0,0" EndPoint="0,4">
                        <GradientStop Color="#10000000" Offset="0" />
                        <GradientStop Color="#10000000" Offset="1" />
                    </LinearGradientBrush>
                </ResourceDictionary>
                <ResourceDictionary x:Key="Dark">
                    <StaticResource x:Key="TitleBarActiveTabPillBackgroundBrush" ResourceKey="ActivePaneInputBackgroundBrush" />
                    <SolidColorBrush x:Key="ActivePaneBorderBrush" Color="Transparent" />
                    <StaticResource x:Key="ActivePaneSurfaceBackgroundBrush" ResourceKey="LayerOnMicaBaseAltFillColorDefaultBrush" />
                    <StaticResource x:Key="InactivePaneSurfaceBackgroundBrush" ResourceKey="LayerOnMicaBaseAltFillColorTransparentBrush" />
                    <StaticResource x:Key="AddressSearchChromeBorderBrush" ResourceKey="ControlStrokeColorSecondaryBrush" />
                    <StaticResource x:Key="InactivePaneBorderBrush" ResourceKey="ControlStrokeColorSecondaryBrush" />
                    <StaticResource x:Key="ActivePaneInputBackgroundBrush" ResourceKey="TextControlBackground" />
                    <SolidColorBrush x:Key="InactivePaneInputBackgroundBrush" Color="Transparent" />
                    <StaticResource x:Key="ActivePaneForegroundBrush" ResourceKey="TextFillColorPrimaryBrush" />
                    <StaticResource x:Key="InactivePaneForegroundBrush" ResourceKey="TextFillColorSecondaryBrush" />
                    <StaticResource x:Key="DetailsHeaderSecondaryForegroundBrush" ResourceKey="TextFillColorTertiaryBrush" />
                    <SolidColorBrush x:Key="SidebarGroupSeparatorBrush" Color="#24FFFFFF" />
                    <LinearGradientBrush x:Key="ExplorerShellPanelBorderBrush" MappingMode="Absolute" StartPoint="0,0" EndPoint="0,4">
                        <GradientStop Color="#10FFFFFF" Offset="0" />
                        <GradientStop Color="#10FFFFFF" Offset="1" />
                    </LinearGradientBrush>
                </ResourceDictionary>
            </ResourceDictionary.ThemeDictionaries>
            <local:LocalizedStrings x:Key="Strings" />
            <Style TargetType="controls:EntryItemHost">
                <Setter Property="Background" Value="Transparent" />
                <Setter Property="PointerOverBackground" Value="{ThemeResource ListViewItemBackgroundPointerOver}" />
                <Setter Property="PressedBackground" Value="{ThemeResource ListViewItemBackgroundPressed}" />
                <Setter Property="SelectedBackground" Value="{ThemeResource ListViewItemBackgroundSelected}" />
                <Setter Property="SelectedPointerOverBackground" Value="{ThemeResource ListViewItemBackgroundSelectedPointerOver}" />
                <Setter Property="SelectedPressedBackground" Value="{ThemeResource ListViewItemBackgroundSelectedPressed}" />
                <Setter Property="IndicatorBrush" Value="{ThemeResource TreeViewItemSelectionIndicatorForeground}" />
                <Setter Property="IndicatorCornerRadius" Value="{ThemeResource ListViewItemSelectionIndicatorCornerRadius}" />
                <Setter Property="IndicatorWidth" Value="3" />
                <Setter Property="AnchorBorderBrush" Value="{ThemeResource TreeViewItemSelectionIndicatorForeground}" />
                <Setter Property="AnchorBorderThickness" Value="1" />
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="controls:EntryItemHost">
                            <Border
                                CornerRadius="{ThemeResource ListViewItemCornerRadius}"
                                MinHeight="{TemplateBinding MinHeight}">
                                <Grid>
                                    <Border
                                        x:Name="BackgroundBorder"
                                        Background="{TemplateBinding Background}"
                                        CornerRadius="{ThemeResource ListViewItemCornerRadius}" />
                                    <Border
                                        x:Name="AnchorBorder"
                                        BorderBrush="{TemplateBinding AnchorBorderBrush}"
                                        BorderThickness="{TemplateBinding AnchorBorderThickness}"
                                        CornerRadius="{ThemeResource ListViewItemCornerRadius}"
                                        Margin="1"
                                        Visibility="Collapsed" />
                                    <Grid Margin="{TemplateBinding Padding}">
                                        <Border
                                            x:Name="SelectionIndicator"
                                            HorizontalAlignment="Left"
                                            Width="{TemplateBinding IndicatorWidth}"
                                            Height="16"
                                            VerticalAlignment="Center"
                                            Margin="0"
                                            Visibility="Collapsed" />
                                        <ContentPresenter
                                            Content="{TemplateBinding Content}"
                                            ContentTemplate="{TemplateBinding ContentTemplate}"
                                            HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                            Margin="0,-1,0,1"
                                            VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                                    </Grid>
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="BreadcrumbMenuFlyoutPresenterStyle" TargetType="MenuFlyoutPresenter">
                <Setter Property="MaxHeight" Value="360" />
            </Style>

            <Style x:Key="CommandMenuFlyoutPresenterStyle" TargetType="MenuFlyoutPresenter" BasedOn="{StaticResource DefaultMenuFlyoutPresenterStyle}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="MenuFlyoutPresenter">
                            <Border
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}"
                                BackgroundSizing="InnerBorderEdge">
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                        <RowDefinition Height="Auto" />
                                    </Grid.RowDefinitions>
                                    <ScrollViewer
                                        x:Name="MenuFlyoutPresenterScrollViewer"
                                        Grid.Row="0"
                                        Margin="{TemplateBinding Padding}"
                                        AutomationProperties.AccessibilityView="Raw"
                                        HorizontalScrollBarVisibility="{TemplateBinding ScrollViewer.HorizontalScrollBarVisibility}"
                                        HorizontalScrollMode="{TemplateBinding ScrollViewer.HorizontalScrollMode}"
                                        IsHorizontalRailEnabled="{TemplateBinding ScrollViewer.IsHorizontalRailEnabled}"
                                        IsVerticalRailEnabled="{TemplateBinding ScrollViewer.IsVerticalRailEnabled}"
                                        VerticalScrollBarVisibility="{TemplateBinding ScrollViewer.VerticalScrollBarVisibility}"
                                        VerticalScrollMode="{TemplateBinding ScrollViewer.VerticalScrollMode}"
                                        ZoomMode="{TemplateBinding ScrollViewer.ZoomMode}">
                                        <ItemsPresenter />
                                    </ScrollViewer>

                                    <Border
                                        x:Name="PART_CommandSeparator"
                                        Grid.Row="1"
                                        Height="1"
                                        Margin="4,4,4,0"
                                        Background="{ThemeResource MenuFlyoutSeparatorBackground}"
                                        Visibility="Collapsed" />

                                    <ItemsControl
                                        x:Name="PART_CommandBar"
                                        Grid.Row="2"
                                        Margin="4,4,4,4"
                                        HorizontalAlignment="Left"
                                        Visibility="Collapsed">
                                        <ItemsControl.ItemsPanel>
                                            <ItemsPanelTemplate>
                                                <StackPanel HorizontalAlignment="Left" Orientation="Horizontal" Spacing="0" />
                                            </ItemsPanelTemplate>
                                        </ItemsControl.ItemsPanel>
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Grid ColumnDefinitions="Auto,Auto">
                                                    <Button
                                                        Width="58"
                                                        Height="52"
                                                        MinWidth="58"
                                                        MinHeight="52"
                                                        Padding="2,2"
                                                        HorizontalAlignment="Stretch"
                                                        VerticalAlignment="Stretch"
                                                        Command="{Binding Command}"
                                                        CommandParameter="{Binding CommandParameter}"
                                                        IsEnabled="{Binding IsEnabled}"
                                                        Style="{StaticResource SubtleButtonStyle}">
                                                        <StackPanel
                                                            HorizontalAlignment="Center"
                                                            Orientation="Vertical"
                                                            Spacing="8">
                                                            <FontIcon
                                                                HorizontalAlignment="Center"
                                                                FontFamily="{Binding IconFontFamily}"
                                                                FontSize="16"
                                                                Glyph="{Binding IconGlyph}" />
                                                            <TextBlock
                                                                HorizontalAlignment="Center"
                                                                FontSize="13"
                                                                LineHeight="13"
                                                                LineStackingStrategy="BlockLineHeight"
                                                                Text="{Binding Label}"
                                                                TextAlignment="Center"
                                                                TextWrapping="NoWrap" />
                                                        </StackPanel>
                                                    </Button>

                                                    <Border
                                                        Grid.Column="1"
                                                        Width="1"
                                                        Height="34"
                                                        Margin="4,0"
                                                        VerticalAlignment="Center"
                                                        Background="{ThemeResource MenuFlyoutSeparatorBackground}"
                                                        Visibility="{Binding SeparatorVisibility}" />
                                                </Grid>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Grid>
                            </Border>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>

            <Style x:Key="ContextMenuFlyoutPresenterStyle" TargetType="MenuFlyoutPresenter" BasedOn="{StaticResource DefaultMenuFlyoutPresenterStyle}" />
            <Style x:Key="ContextMenuFlyoutItemStyle" TargetType="MenuFlyoutItem" BasedOn="{StaticResource DefaultMenuFlyoutItemStyle}" />
            <Style x:Key="ContextMenuFlyoutSubItemStyle" TargetType="MenuFlyoutSubItem" BasedOn="{StaticResource DefaultMenuFlyoutSubItemStyle}">
                <Setter Property="Template">
                    <Setter.Value>
                        <ControlTemplate TargetType="MenuFlyoutSubItem">
                            <Grid
                                x:Name="LayoutRoot"
                                Margin="{StaticResource MenuFlyoutItemMargin}"
                                Padding="{TemplateBinding Padding}"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="{TemplateBinding CornerRadius}">
                                <VisualStateManager.VisualStateGroups>
                                    <VisualStateGroup x:Name="CommonStates">
                                        <VisualState x:Name="Normal" />
                                        <VisualState x:Name="PointerOver">
                                            <VisualState.Setters>
                                                <Setter Target="LayoutRoot.Background" Value="{ThemeResource MenuFlyoutSubItemBackgroundPointerOver}" />
                                                <Setter Target="TextBlock.Foreground" Value="{ThemeResource MenuFlyoutSubItemForegroundPointerOver}" />
                                                <Setter Target="SubItemChevron.Foreground" Value="{ThemeResource MenuFlyoutSubItemChevronPointerOver}" />
                                                <Setter Target="IconContent.Foreground" Value="{ThemeResource MenuFlyoutSubItemForegroundPointerOver}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Pressed">
                                            <VisualState.Setters>
                                                <Setter Target="LayoutRoot.Background" Value="{ThemeResource MenuFlyoutSubItemBackgroundPressed}" />
                                                <Setter Target="TextBlock.Foreground" Value="{ThemeResource MenuFlyoutSubItemForegroundPressed}" />
                                                <Setter Target="SubItemChevron.Foreground" Value="{ThemeResource MenuFlyoutSubItemChevronPressed}" />
                                                <Setter Target="IconContent.Foreground" Value="{ThemeResource MenuFlyoutSubItemForegroundPressed}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="SubMenuOpened">
                                            <VisualState.Setters>
                                                <Setter Target="LayoutRoot.Background" Value="{ThemeResource MenuFlyoutSubItemBackgroundSubMenuOpened}" />
                                                <Setter Target="TextBlock.Foreground" Value="{ThemeResource MenuFlyoutSubItemForegroundSubMenuOpened}" />
                                                <Setter Target="SubItemChevron.Foreground" Value="{ThemeResource MenuFlyoutSubItemChevronSubMenuOpened}" />
                                                <Setter Target="IconContent.Foreground" Value="{ThemeResource MenuFlyoutSubItemForegroundSubMenuOpened}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="Disabled">
                                            <VisualState.Setters>
                                                <Setter Target="LayoutRoot.Background" Value="{ThemeResource MenuFlyoutSubItemBackgroundDisabled}" />
                                                <Setter Target="TextBlock.Foreground" Value="{ThemeResource MenuFlyoutSubItemForegroundDisabled}" />
                                                <Setter Target="SubItemChevron.Foreground" Value="{ThemeResource MenuFlyoutSubItemChevronDisabled}" />
                                                <Setter Target="IconContent.Foreground" Value="{ThemeResource MenuFlyoutSubItemForegroundDisabled}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                    <VisualStateGroup x:Name="CheckPlaceholderStates">
                                        <VisualState x:Name="NoPlaceholder" />
                                        <VisualState x:Name="CheckPlaceholder">
                                            <VisualState.Setters>
                                                <Setter Target="TextBlock.Margin" Value="{ThemeResource MenuFlyoutItemPlaceholderThemeThickness}" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="IconPlaceholder">
                                            <VisualState.Setters>
                                                <Setter Target="TextBlock.Margin" Value="{ThemeResource MenuFlyoutItemPlaceholderThemeThickness}" />
                                                <Setter Target="IconRoot.Visibility" Value="Visible" />
                                            </VisualState.Setters>
                                        </VisualState>
                                        <VisualState x:Name="CheckAndIconPlaceholder">
                                            <VisualState.Setters>
                                                <Setter Target="TextBlock.Margin" Value="{ThemeResource MenuFlyoutItemDoublePlaceholderThemeThickness}" />
                                                <Setter Target="IconRoot.Margin" Value="{ThemeResource MenuFlyoutItemPlaceholderThemeThickness}" />
                                                <Setter Target="IconRoot.Visibility" Value="Visible" />
                                            </VisualState.Setters>
                                        </VisualState>
                                    </VisualStateGroup>
                                    <VisualStateGroup x:Name="PaddingSizeStates">
                                        <VisualState x:Name="DefaultPadding" />
                                        <VisualState x:Name="NarrowPadding">
                                            <Storyboard>
                                                <ObjectAnimationUsingKeyFrames Storyboard.TargetName="LayoutRoot" Storyboard.TargetProperty="Padding">
                                                    <DiscreteObjectKeyFrame KeyTime="0" Value="{StaticResource MenuFlyoutItemThemePaddingNarrow}" />
                                                </ObjectAnimationUsingKeyFrames>
                                            </Storyboard>
                                        </VisualState>
                                    </VisualStateGroup>
                                </VisualStateManager.VisualStateGroups>
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <Viewbox
                                        x:Name="IconRoot"
                                        Grid.Column="0"
                                        Width="16"
                                        Height="16"
                                        HorizontalAlignment="Left"
                                        VerticalAlignment="Center"
                                        Visibility="Collapsed">
                                        <ContentPresenter
                                            x:Name="IconContent"
                                            Content="{TemplateBinding Icon}"
                                            Foreground="{TemplateBinding Foreground}" />
                                    </Viewbox>
                                    <TextBlock
                                        x:Name="TextBlock"
                                        Grid.Column="0"
                                        HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                        VerticalAlignment="{TemplateBinding VerticalContentAlignment}"
                                        Foreground="{TemplateBinding Foreground}"
                                        Text="{TemplateBinding Text}"
                                        TextTrimming="{ThemeResource MenuFlyoutItemTextTrimming}" />
                                    <FontIcon
                                        x:Name="SubItemChevron"
                                        Grid.Column="1"
                                        Margin="72,0,0,0"
                                        AutomationProperties.AccessibilityView="Raw"
                                        FontFamily="{ThemeResource SymbolThemeFontFamily}"
                                        FontSize="12"
                                        Foreground="{ThemeResource MenuFlyoutSubItemChevron}"
                                        Glyph="&#xE974;"
                                        MirroredWhenRightToLeft="True" />
                                </Grid>
                            </Grid>
                        </ControlTemplate>
                    </Setter.Value>
                </Setter>
            </Style>
            <Style x:Key="CompactSidebarMenuFlyoutPresenterStyle" TargetType="MenuFlyoutPresenter">
                <Setter Property="MinWidth" Value="240" />
                <Setter Property="Shadow" Value="{x:Null}" />
                <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
                <Setter Property="ScrollViewer.VerticalScrollMode" Value="Auto" />
                <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Disabled" />
                <Setter Property="ScrollViewer.HorizontalScrollMode" Value="Disabled" />
            </Style>
            <!-- Other app resources here -->
        </ResourceDictionary>
    </Application.Resources>
</Application>
