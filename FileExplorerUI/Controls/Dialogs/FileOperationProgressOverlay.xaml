<UserControl
    x:Class="FileExplorerUI.Controls.FileOperationProgressOverlay"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid x:Name="OverlayRoot" Background="{ThemeResource ContentDialogSmokeFill}">
        <Border
            Width="{ThemeResource ContentDialogMaxWidth}"
            MinWidth="{ThemeResource ContentDialogMinWidth}"
            HorizontalAlignment="Center"
            VerticalAlignment="Center"
            Background="{ThemeResource ContentDialogBackground}"
            BorderBrush="{ThemeResource ContentDialogBorderBrush}"
            BorderThickness="{ThemeResource ContentDialogBorderWidth}"
            CornerRadius="12">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <Border
                    Background="{ThemeResource ContentDialogTopOverlay}"
                    BorderBrush="{ThemeResource ContentDialogSeparatorBorderBrush}"
                    BorderThickness="{ThemeResource ContentDialogSeparatorThickness}">
                    <StackPanel Padding="{ThemeResource ContentDialogPadding}" Spacing="12">
                        <TextBlock
                            x:Name="TitleTextBlock"
                            Margin="{ThemeResource ContentDialogTitleMargin}"
                            FontSize="20"
                            FontWeight="SemiBold"
                            TextWrapping="Wrap" />
                        <TextBlock
                            x:Name="MessageTextBlock"
                            TextWrapping="Wrap" />
                        <TextBlock
                            x:Name="CurrentItemTextBlock"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                            TextTrimming="CharacterEllipsis"
                            TextWrapping="NoWrap" />
                        <ProgressBar
                            x:Name="OperationProgressBar"
                            Minimum="0"
                            Maximum="1"
                            Value="0" />
                        <TextBlock
                            x:Name="ProgressTextBlock"
                            HorizontalAlignment="Right"
                            Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                    </StackPanel>
                </Border>

                <Border
                    Grid.Row="1"
                    Background="{ThemeResource ContentDialogBackground}"
                    Padding="{ThemeResource ContentDialogPadding}">
                    <Button
                        x:Name="CancelButton"
                        Height="36"
                        HorizontalAlignment="Stretch"
                        UseSystemFocusVisuals="False"
                        Click="CancelButton_Click" />
                </Border>
            </Grid>
        </Border>
    </Grid>
</UserControl>
