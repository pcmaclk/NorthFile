# 设置功能实现进度

## 当前状态

设置页框架、导航、本地化和第一批真实设置项已经接通。当前阶段的重点已经从“把入口做出来”转到“把已存在设置做实”和“补齐剩余占位功能”。

## 已接入并可持久化的设置

### 通用

- 语言
- 启动位置
- 收藏分组显示
- 云盘分组显示
- 网络分组显示
- 标签分组显示

### 外观

- 主题
- 默认排序
- 默认分组

### 文件和文件夹

- 显示隐藏文件
- 显示受保护的系统文件
- 显示点开头文件
- 显示文件扩展名
- 删除时显示确认对话框

### 高级

- 导入设置
- 导出设置
- 开机自启动
- 收缩到托盘
- 窗口尺寸记忆

## 本轮补充完成

- 窗口尺寸记忆链路增加诊断日志，并改为在关闭窗口和最小化到托盘时直接采样真实窗口尺寸后保存。
- 深色模式标题栏主题改为跟随窗口实际主题，不再依赖应用默认模式。
- 侧边栏 `收藏 / 云盘 / 网络 / 标签` 分组内条目统一收敛到 `EntryItemHost` 样式体系，选中指示条高度与 TreeView 对齐。
- 设置文案同步更新，去掉已经落地功能上的“占位”描述，并把仅控制侧边栏显隐的项改成更准确的表述。

## 仍未做实的设置项

- 默认文件管理器
- 快捷键
- 标签能力本身

## 半完成项

- 云盘分组显示
  目前主要是控制侧边栏中的静态 `OneDrive` 分组项显隐，不是完整云盘集成。

- 网络分组显示
  目前主要是控制侧边栏分组显隐，尚未接入真实网络位置枚举。

- 标签分组显示
  目前主要是控制侧边栏中的静态标签项显隐，不是完整标签系统。

## 涉及代码

- `FileExplorerUI/Settings/AppSettings.cs`
- `FileExplorerUI/Settings/AppSettingsService.cs`
- `FileExplorerUI/Controls/SettingsView.xaml`
- `FileExplorerUI/Controls/SettingsView.xaml.cs`
- `FileExplorerUI/MainWindow.xaml`
- `FileExplorerUI/MainWindow.xaml.cs`
- `FileExplorerUI/SidebarView.xaml`
- `FileExplorerUI/SidebarView.xaml.cs`
- `FileExplorerUI/App.xaml`

## 下一步建议顺序

1. 默认文件管理器
2. 快捷键
3. 标签能力本身
4. 云盘 / 网络真实数据接入
