<UserControl
    x:Class="NorthFileUI.Controls.ExplorerPaneToolbarLayout"
    x:Name="RootControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <Grid
        Height="{x:Bind LayoutHeight, Mode=OneWay}"
        ColumnSpacing="{x:Bind ColumnSpacing, Mode=OneWay}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="5*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <ContentPresenter
            Grid.Column="0"
            VerticalAlignment="Stretch"
            Content="{x:Bind LeadingContent, Mode=OneWay}" />

        <ContentPresenter
            Grid.Column="1"
            VerticalAlignment="Stretch"
            Content="{x:Bind CenterContent, Mode=OneWay}" />

        <ContentPresenter
            Grid.Column="2"
            VerticalAlignment="Stretch"
            Content="{x:Bind TrailingContent, Mode=OneWay}" />
    </Grid>
</UserControl>
